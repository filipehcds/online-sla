spring:
  profiles: runtime
  cloud:
    inetutils:
      use-only-site-local-interfaces: true
      ignoredInterfaces:
        - docker0
        - veth.*
    consul:
      host: consul_host
      port: ${CONSUL_PORT}
      enabled: true
      discovery:
       healthCheckInterval: 15s
       enabled: true
       register: false